[project]
name = "ner-flood"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.3",
    "asyncio>=4.0.0",
    "duckdb>=1.5.5",
    "lmoments3>=1.0.8",
    "numpy>=2.5.3",
]

[dependency-groups]
dev = [
    "hypothesis>=6.168.0",
    "pytest>=9.1.1",
]
